select 'hello';

select 'hello' as "Greeting";

show databases;

show schemas; -- Exibe os schemas no escopo atual (database)

show schemas in account; -- Igual a show schemas, mas procura em toda a conta, sem escopo
